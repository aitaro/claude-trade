services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-claude_trade}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-claude_trade}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-claude_trade}"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: journald
      options:
        tag: postgres
    restart: unless-stopped

  ib-gateway:
    image: ghcr.io/gnzsnz/ib-gateway:stable
    entrypoint: ["/bin/sh", "/opt/ib-entrypoint.sh"]
    volumes:
      - ../scripts/ib-gateway-entrypoint.sh:/opt/ib-entrypoint.sh:ro
    logging:
      driver: journald
      options:
        tag: ib-gateway
    environment:
      TWS_USERID: ${TWS_USERID}
      TWS_PASSWORD: ${TWS_PASSWORD}
      TRADING_MODE: paper
      TWS_ACCEPT_INCOMING: "true"
      TWOFA_TIMEOUT_ACTION: restart
      READ_ONLY_API: "no"
      # primary = 他セッションが来ても自分は死なない（Gateway 常駐向け）
      # primaryoverride = 他セッションが来たら自分が退く（逆なので注意！名前に騙されるな）
      EXISTING_SESSION_DETECTED_ACTION: primary
      IB_RESTART_DELAY: "180"
    network_mode: host
    restart: unless-stopped

volumes:
  pgdata:
